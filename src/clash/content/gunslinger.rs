use std::collections::HashMap;

use super::super::SkillInfo;

pub fn get_weapon_skills() -> Vec<&'static str> {
    vec![]
}

pub fn add_gunslinger_skills(m: &mut HashMap<&'static str, SkillInfo>) {}
